"use client";

import Hero from "@/components/Hero";
import SplatGrid from "@/components/SplatGrid";

export default function Home() {
  return (
    <main>
      <Hero />
      <SplatGrid />
    </main>
  );
}